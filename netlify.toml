[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist" # O "build" si usas Create React App

[functions]
  directory = "../backend"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
