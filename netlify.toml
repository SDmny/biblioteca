[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist" # O "build" si usas Create React App

[functions]
  directory = "../backend"

[[redirects]]
  from = "/api/*"
  to = "/index.html"
  status = 200


